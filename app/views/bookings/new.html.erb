hello i am new
<%= simple_form_for [@car, @booking] do |f| %>
  <%= f.input :starting_date %>
  <%= f.input :ending_date %>
  <%= f.submit class: "btn btn-primary border rounded-pill" %>
  </nav>
<% end %>
<nav>
