<div class="container-fluid mt-3" >
  <div class="card-car gap-3">

    <% @cars.each do |car| %>
      <div class="card rounded-4 rounded border border-2 border-black-subtitle col-sm-12" style="background-color: #ffffff;">
        <%= link_to car_path(car) do %>
          <div class="card-body" >
            <p>
              <%= image_tag ("http://loremflickr.com/280/280/#{car.car_brand}#{car.car_model}"), class: "card-img-top rounded-4" %>
            </p>
            <p>
              <%= car.car_brand %>
              <%= car.car_model %>
            </p>
            <p>
              <%= car.price %>€/jour
            </p>
            <p>
              <%= car.user.first_name %>
              <%= car.user.last_name %>
              (@<%= car.user.username %>)
            </p>
          </div>
        <% end %>
      </div>

    <% end %>

  </div>
</div>
